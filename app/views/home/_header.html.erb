<%= stylesheet_link_tag "header" %>
<nav class="navbar navbar-expand-lg bg-body-tertiary" data-bs-theme="dark">
  <div class="container-fluid">
    <%= link_to "SecretSpots", root_path, class:"navbar-brand" %>
    <!-- <a class="navbar-brand" href="/">SecretSpots</a> -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">

        <!-- Secrets -->
        <li class="nav-item">
          <%= link_to t('global.secrets'), secrets_path, class:"nav-link active" %>
        </li>

        <!-- Followers list -->
        <li class="nav-item">
          <%= link_to t('global.volgers'), root_path , class:"nav-link active" %>
        </li>

        <!-- Choose language -->
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            <%= t('.language') %>
          </a>
          <ul class="dropdown-menu">
            <!-- https://www.youtube.com/watch?v=4m64FHUrEhg -->
            <li><a class="dropdown-item" href=<%= root_path(locale: :en) %>>English</a></li>
            <li><a class="dropdown-item" href=<%= root_path(locale: :nl) %>>Nederlands</a></li>
          </ul>
        </li>

        <!-- Item
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="#">Home</a>
        </li>
        -->

        <!-- Link
        <li class="nav-item">
          <a class="nav-link" href="#">Link</a>
        </li>
        -->

        <!-- Dropdown
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Dropdown
          </a>
          <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="#">Action</a></li>
            <li><a class="dropdown-item" href="#">Another action</a></li>
            <li><hr class="dropdown-divider"></li>
            <li><a class="dropdown-item" href="#">Something else here</a></li>
          </ul>
        </li>
        -->

        <!-- Disabled
        <li class="nav-item">
          <a class="nav-link disabled" aria-disabled="true">Disabled</a>
        </li>
        -->

      </ul>



      <div class="d-flex navbar-text">
        <% if user_signed_in? %> 
          <div id="welcome"> <%= t('.welcome') %> <%= current_user.username %> </div>

          <% if current_user.profile_picture.attached? %>
            <%= link_to image_tag(current_user.profile_picture) , user_path(current_user.id), method: :get,class:"user_profpic"  %>
          <% else %>
            <%= link_to image_tag("/assets/empty-profile-picture.png"), user_path(current_user.id), method: :get,class:"user_profpic"  %>

          <% end %>

          <%= button_to t('.log-out'), destroy_user_session_path, method: :delete %> 

        <% else %>

          <%= button_to t('.log-in'), new_user_session_path,method: :get %> 
          <%= button_to t('.sign-up'), new_user_registration_path,method: :get %>
        <% end %>
      </div>

    </div>
  </div>
</nav>